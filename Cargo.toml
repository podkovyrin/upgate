[package]
name = "brew-delay-upgrade"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"

[lints.rust]
unsafe_code = "warn"

[lints.clippy]
all = "warn"
pedantic = "warn"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
